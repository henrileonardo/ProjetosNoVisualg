algoritmo "Contando até 10"

var

   cdt : inteiro

inicio

      cdt <- 0
      enquanto (cdt <= 10) faca
               escreval (cdt)
               cdt <- cdt + 1
      fimenquanto


fimalgoritmo